import Size = require('common/size');

class Settings {
    static readonly MAIN_COLOR: string = '#f9c731';
    static readonly RASTER_SIZE: Size = new Size(50, 50);
    static readonly SPRTIES_LOCATION: string = 'sprites';
}

export = Settings;